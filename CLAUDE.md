# CLAUDE.md - genesis-child

Lee y sigue las instrucciones de `AGENTS.md` en la raiz del repositorio.

Notas rapidas para Claude Code:
- Aplica solo lo relevante de `AGENTS.md` a la tarea actual, priorizando rendimiento y accesibilidad del tema hijo.
- Prefiere los hooks/helpers existentes de Genesis/WordPress antes de crear nuevos; prefija codigo propio con `reban_`.
- No ejecutes comandos git (commit/push/branch) a menos que el usuario lo solicite; sugiere el comando si hace falta.
